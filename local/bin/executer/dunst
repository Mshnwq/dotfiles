#!/usr/bin/env bash

CONF="$HOME/.config/dunst/dunstrc"
[[ -s $CONF ]] || exit 1
app="dunst"

case $1 in
--kill | --start) pkill "$app" 2>/dev/null ;;&
--start) nohup "$app" -config \
  "$HOME/.config/dunst/dunstrc" &>/dev/null & ;;
*) exit 2 ;;
esac
