#!/usr/bin/env bash

[[ -n $YDOTOOL_SOCKET ]] || exit 1
app="ydotool"

case $1 in
--kill | --start) sudo \
  pkill "$app" 2>/dev/null ;;&
--start) sudo \
  --background "${app}d" \
  --socket-path="$YDOTOOL_SOCKET" \
  --socket-own="$(id -u):$(id -g)" ;;
*) exit 2 ;;
esac
